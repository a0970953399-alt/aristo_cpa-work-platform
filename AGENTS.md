# Codex 專案協作規則

## 交接流程

- 每次開始處理本專案前，先閱讀 `.codex/HANDOFF.md`，再檢查 `git status` 與最新提交。
- 每次完成一段 vibe coding 工作後，更新 `.codex/HANDOFF.md`，讓另一台電腦的 Codex 能直接接手。
- `HANDOFF.md` 只保留當下最新狀態，不要累積逐日流水帳；過往內容由 Git 歷史保存。
- 交接內容應包含：目前基準版本、本次完成事項、已確認決策、待辦事項、驗證結果、部署狀態、風險與相關檔案。
- 不要在交接文件寫入 Gmail、OAuth Client secret、Firebase secret、refresh token、PIN 或其他敏感資料。
- 若使用者要求推送 GitHub，將本次相關程式碼與最新交接文件一起提交到 `main`；不要夾帶無關檔案。

## 專案原則

- 保留既有 Firebase 資料與薪資、零用金、工時、工作矩陣等業務邏輯，除非任務明確要求修改。
- 修改前先理解既有資料格式與模組邊界，避免建立重複的人員、事件或業務資料。
- 根目錄正式驗證使用 `npm.cmd run build`；Functions 修改另在 `functions` 目錄執行 `npm.cmd run build`。
- Google 日曆目前是平台單向同步到 Google，不要自行改成雙向同步。
- 原有 `firebase/firestore-send-email` Extension 不屬於自建 Functions，部署時不得刪除或取代。

## 修改前的影響檢查

- 先追查需求涉及的畫面、共用函式、資料讀寫、登入身分、Firestore Rules 與 Functions，說明可能受影響的既有流程；不要只檢查使用者指定的畫面。
- 採用符合現有架構且範圍最小的修改，不做無關重構。若必須改變既有業務行為或修復正式資料，先說明具體差異與影響；超出已授權範圍時再取得同意。
- 主動檢查舊資料缺少欄位、空值、型別差異與預設值，確保前端、後端及權限規則的判斷一致。Firestore Rules 不可直接假定選填欄位存在；需要相容處理時，使用符合既有語意的預設值，不能以放寬權限掩蓋問題。
- 依本次修改檢查相關角色、啟用／停用、本人／他人資料、已結算／未結算等條件。例如改動主管補登工時時，必須同時驗證員工原有的正常上下班打卡。

## 除錯與回歸驗證

- 除錯先蒐集證據：最後正常時間、異常開始時間、受影響與正常案例的差異、近期 Git 修改、實際部署版本及錯誤訊息。明確區分已確認根因與待驗證推測，不因時間接近就認定某筆提交是原因。
- 盡可能先重現問題，再用同一案例驗證修正，並執行相關原有功能的回歸測試。依影響範圍安排測試；純文件修改不必重跑應用程式建置。
- 共用權限或資料格式變更需涵蓋舊資料與不同角色的正反案例，確認正常操作被允許、越權及受保護資料修改仍被拒絕。必要時加入可重複執行的測試。
- 非同步寫入需處理錯誤、復原操作狀態，並依需求防止重複送出；成功提示應以資料保存確認為準。通知等附帶操作失敗不應掩蓋主要操作已成功的結果。
- 建置成功只代表建置通過，不代表登入、實際操作或多人同步已驗證。回報實際執行的測試、結果與未驗證項目，不宣稱零 bug 或把模擬測試當作正式帳號實測。
- 優先使用模擬資料或測試環境。正式資料排查先採唯讀方式，不以批次補欄位、改寫紀錄或放寬規則掩蓋程式錯誤；需要資料修復時先列出影響。

## 部署與交接核對

- 分別記錄前端、Firestore Rules 與 Functions 是否需要部署及是否已部署。GitHub 已推送不等於 Firebase 已部署，交接文件也不能取代正式環境的實際核對。
- 在已授權部署的範圍內，只部署必要服務；部署前確認實際差異，部署後核對成功結果，必要時讀回正式版本。保留可追溯的修正前版本與修正原因。
- 工作結束更新交接文件，保留當下最新基準、完成事項、已確認決策、待辦、測試結果、各服務部署狀態與剩餘風險；移除已失效的待辦，尚未查證的歷史事項應標示待核對，不能當成現在的部署指令。
